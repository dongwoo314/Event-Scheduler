version: 1.0
runtime: nodejs16
build:
  commands:
    build:
      - npm install
      - echo "Backend build completed"
run:
  runtime-version: 16
  command: node server.js
  network:
    port: 3001
    env: PORT
  env:
    - name: NODE_ENV
      value: "production"
